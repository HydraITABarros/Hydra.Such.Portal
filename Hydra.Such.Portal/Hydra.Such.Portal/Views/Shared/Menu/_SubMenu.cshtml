@using Hydra.Such.Data.ViewModel
@model List<MenuViewModel>

<ul class="submenu">    
    
    @if(Model.Count() > 0)
    {
        @foreach (MenuViewModel menuItem in Model)
        {
            @Html.Partial("Menu/_SubMenuItem", (MenuViewModel)menuItem)
        }
    }
</ul>